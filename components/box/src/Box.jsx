import styled from 'styled-components/macro';
import { space, layout, typography, color, flexbox, border } from 'styled-system'

const Box = styled.div`
  ${space}
  ${color}
  ${typography}
  ${layout}
  ${flexbox}
  ${border}
`

export default Box;
